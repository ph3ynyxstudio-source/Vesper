// L'INTERRUPTEUR DE L'APPLICATION (MVP LOCK)
// false = L'application utilise les mocks (sécurisé, idéal pour coder l'UI)
// true  = L'application se connecte à Tauri et attend le backend Rust
export const USE_RUST = false;

